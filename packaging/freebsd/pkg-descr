Интеграция sing-box с OPNsense.

Пакет устанавливает бинарный файл sing-box, WebUI, базовую MVC-модель настроек,
действия configd, службу rc.d, безопасные шаблоны конфигурации, механизм
восстановления после сетевых изменений и метаданные меню OPNsense.
